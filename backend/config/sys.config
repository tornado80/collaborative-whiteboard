[
  {backend, [
    % use the following line to set the app data directory where databases will be stored
    % {app_data_directory, "/path/to/data/directory"}
  ]},
  {lager, [
    {handlers, [
      {lager_console_backend, [{level, info}]}
    ]}
  ]}
].
